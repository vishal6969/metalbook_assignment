import * as React from 'react';
import {View, Text} from 'react-native';

const Dashboard = () => {
  return (
    <View
      style={{
        flex: 1,
      }}>
      <Text>Dashboard</Text>
    </View>
  );
};

export default Dashboard;
